// main.js

// some fun or whatever
{
  /**
   * what
   * is
   * ts
   * */
  let foo = console.log("hello world");
  foo;

  let bar = "idiot";
  console.log(Math.abs(Number(bar)));
  console.log(+bar);
}
